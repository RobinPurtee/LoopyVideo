<UserControl
    x:Class="WebAppServiceClient.Controls.IndicatorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebAppServiceClient.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="100">
    <UserControl.Resources>
        <Color x:Key="LightOnColor">Green</Color>
        <SolidColorBrush x:Key="LightOffColor">Red</SolidColorBrush>
        <Style TargetType="Ellipse">
            <Setter Property="Stroke" Value="Black"/>
            
            <Setter Property="Fill" Value="{StaticResource LightOffColor}"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="On"/>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <TextBlock x:Name="textBlock" Text="{Binding Path=Label}"/>
        <Ellipse  
            Width="{Binding Height, ElementName=textBlock}" 
            Height="{Binding Height, ElementName=textBlock}"
            >
        </Ellipse>


    </StackPanel>
</UserControl>
